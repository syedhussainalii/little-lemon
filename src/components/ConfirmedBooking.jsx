const ConfirmedBooking = () => {
  return (
    <section
      className="card"
      aria-labelledby="confirmed-booking-title"
      aria-live="polite"
    >
      <h2 id="confirmed-booking-title">Booking Confirmed</h2>
      <p>Your reservation has been successfully submitted.</p>
    </section>
  );
};

export default ConfirmedBooking;
